// Main API resource schemas (User, Segment, Campaign, Journey, Template,
// Event, List, Subscription, Tag, Project, ApiKey, WebhookEndpoint).
// Populated as each controller in spotzee-js-app migrates onto defineRoute.
export {}
